--- a/src/AV/AVWrapper.cpp	2025-09-28 19:26:49.000000000 +0200
+++ b/src/AV/AVWrapper.cpp	2026-08-14 03:07:10.412577022 +0200
@@ -145,7 +145,14 @@
 }
 
 bool AVCodecSupportsPixelFormat(const AVCodec* codec, AVPixelFormat pixel_fmt) {
+#if LIBAVCODEC_VERSION_INT >= AV_VERSION_INT(61, 13, 100)
+	const AVPixelFormat *p = NULL;
+	if(avcodec_get_supported_config(NULL, codec, AV_CODEC_CONFIG_PIX_FORMAT, 0,
+					(const void **)&p, NULL) < 0)
+		p = NULL;
+#else
 	const AVPixelFormat *p = codec->pix_fmts;
+#endif
 	if(p == NULL)
 		return true; // NULL means 'unknown' or 'any', assume it is supported
 	while(*p != AV_PIX_FMT_NONE) {
@@ -157,7 +164,14 @@
 }
 
 bool AVCodecSupportsSampleFormat(const AVCodec* codec, AVSampleFormat sample_fmt) {
+#if LIBAVCODEC_VERSION_INT >= AV_VERSION_INT(61, 13, 100)
+	const AVSampleFormat *p = NULL;
+	if(avcodec_get_supported_config(NULL, codec, AV_CODEC_CONFIG_SAMPLE_FORMAT, 0,
+					(const void **)&p, NULL) < 0)
+		p = NULL;
+#else
 	const AVSampleFormat *p = codec->sample_fmts;
+#endif
 	if(p == NULL)
 		return true; // NULL means 'unknown' or 'any', assume it is supported
 	while(*p != AV_SAMPLE_FMT_NONE) {
